namespace Domain.Enums;

public enum SubmissionType
{
    DemoUpload = 1,              // Label submission (Purple Crunch Records)
    ArtistInformation = 2,       // Record label
    SongwriterInformation = 3,   // Publishing only
    SyncRequest = 4,             // Shared / Sync
    GeneralContactInquiry = 5,   // General contact
    SupportForm = 6,             // Support
    LegalRequest = 7             // PNG site only
}
